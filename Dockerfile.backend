FROM node:20-alpine
WORKDIR /app
COPY package*.json ./
RUN npm install --production
COPY server/ ./server/
# Copy front directory for content extraction and initial data
COPY front/ ./front/
EXPOSE 5000
ENV PORT=5000
ENV NODE_ENV=production
CMD ["node", "server/index.cjs"]
